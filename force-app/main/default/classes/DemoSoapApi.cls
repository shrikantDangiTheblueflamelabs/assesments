global with sharing class DemoSoapApi {
//   webService static List<Lead> getLeads(Integer leadsLimit) {
//     return [SELECT Id, FirstName, LastName FROM Lead LIMIT :leadsLimit];
//   }

  webService static void getLeads() {
    Integer i = 1;
    // return [SELECT Id, FirstName, LastName FROM Lead LIMIT :leadsLimit];
  }
}
